import 'package:flutter/material.dart';

// load in current node > root passed from home or traversed through children
// only show back if parent is not null
// title = tree notes if parent is null else do current node name
// + button only allow leafs if parent is not null

class Tree extends StatefulWidget {
  @override
  _TreeState createState() => _TreeState();
}

class _TreeState extends State<Tree> {
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}